enum AuthStatus {
    LOCKED
    EMAIL_UNVERIFIED
    VERIFIED
}

model Account {
    id String @id
    email String @unique @db.VarChar(255)
    password String @db.VarChar(255)
    status AuthStatus @default(VERIFIED)
    
    refreshToken RefreshToken[]
    account_role AccountRole[]
}